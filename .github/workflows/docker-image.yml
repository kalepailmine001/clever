name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  infinite-curl:
    runs-on: macos-latest
    steps:
      - name: Watch
        run: |
          brew install tmate
          tmate
